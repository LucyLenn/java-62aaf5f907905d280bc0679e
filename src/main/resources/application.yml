server:
  port: 8080

logging:
  level:
    ua.hillel.homeworks.homework34: INFO
    org.springframework.web: INFO
  file:
    path: log/

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/homework32
    username: postgres
    password: password
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: none
